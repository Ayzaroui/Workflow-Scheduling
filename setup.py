from setuptools import setup, find_packages

setup(
    name="workflow_scheduling",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Liste des dépendances ici
    ],
)